"use client";

// eslint-disable-next-line @next/next/no-img-element
export default function Logo() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/ultimo.svg"
      alt="ACORVI Logo"
      style={{ height: "40px" }}
    />
  );
}
